Open a workflow and enable the **Run in Queue** option when you want automated trigger launches to be processed asynchronously.

Manual executions from the UI and trigger test calls remain synchronous so operators can get immediate feedback.