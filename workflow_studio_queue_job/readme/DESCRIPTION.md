This addon extends Workflow Studio with optional `queue_job` integration.

When enabled on a workflow, automated trigger executions are intercepted and dispatched asynchronously through `queue_job`, while manual launches and test-mode executions remain synchronous.